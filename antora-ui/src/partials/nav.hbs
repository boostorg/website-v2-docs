<div class="card nav-container"{{#if page.component}} data-component="{{page.component.name}}" data-version="{{page.version}}"{{/if}}>
  <aside class="nav">
    <button class="nav-close"></button>
    <div class="panels">
      {{> nav-menu}}
    </div>
  </aside>
</div>
